select * from IDENTITIES

CREATE TABLE USERS
     (UID VARCHAR(100) NOT NULL, PASSWORD VARCHAR(100) NOT NULL,
	EMAIL VARCHAR(100) NOT NULL, PRIMARY KEY (EMAIL, UID));
	
select * from USERS

INSERT INTO USERS 
VALUES ('ROOT','PASSWORD','ROOT@ROOT.COM')